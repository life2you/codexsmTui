# Homebrew Tap Assets

This directory contains the generated Homebrew formula for `codexsmTui`.

Update it after pushing a release tag:

```bash
./scripts/update-homebrew-formula.sh
```

The generated formula is intended to be copied into the separate `life2you/homebrew-tap` repository.
